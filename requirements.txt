# Core web framework
fastapi
uvicorn[standard]
pydantic

# Database
SQLAlchemy

# Auth
bcrypt
PyJWT

# Config
python-dotenv

# HTTP client (live pricing forex, image fetching, RAG knowledge base helpers)
requests

# LLM agent (only one of these is required depending on which key you set
# in .env -- ANTHROPIC_API_KEY or GROQ_API_KEY. Safe to keep both installed.)
anthropic
groq

# RAG (Retrieval-Augmented Generation) -- rag/embedder.py falls back to
# TF-IDF automatically if sentence-transformers/faiss aren't installed, but
# for the better semantic search experience, install these too:
numpy
scikit-learn
sentence-transformers
faiss-cpu

# Core web framework
fastapi
uvicorn[standard]
pydantic

# Database
SQLAlchemy

# Auth
bcrypt
PyJWT

# Config
python-dotenv

# HTTP client (live pricing forex, image fetching, RAG knowledge base helpers)
requests

# LLM agent (only one of these is required depending on which key you set
# in Vercel env vars -- ANTHROPIC_API_KEY or GROQ_API_KEY)
anthropic
groq

# ML / RAG (RAG falls back to TF-IDF automatically; explain_budget.py uses
# its built-in Shapley explainer when shap is not installed)
numpy
pandas
joblib
scikit-learn==1.9.0